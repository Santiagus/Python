[pytest]
testpaths = tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
markers =
    unit: Isolated in-memory unit tests
    integration: Database, Celery, and Redis integration tests
    e2e: Live multi-process end-to-end tests

